{
  "name": "@chia/wallet-connect-demo",
  "version": "0.0.2",
  "private": true,
  "scripts": {
    "dev": "npm run build:package && next dev",
    "dev:package": "cd .. && npm run build",
    "build:package": "cd .. && npm run build",
    "build": "npm run build:package && next build",
    "preview": "npm run build && next start",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "@maximedogawa/chia-wallet-connect-react": "file:../dist",
    "next": "^16.0.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}

